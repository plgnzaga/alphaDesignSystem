# React + TypeScript + Vite + Material UI

To run locally: "npm run storybook" or "npm run sb" to view documentation

To Use as theme:
1. Place theme folder inside src folder for each application.
2. Enclose app.js or index.js with ThemeProvider and import alphaTheme.

